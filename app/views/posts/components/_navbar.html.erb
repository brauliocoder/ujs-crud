<nav class="sticky-top navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="#">Posts</a>

    <%= form_tag(posts_path, :method => "get", class: "d-flex", remote: true) do %>
      <%= text_field_tag(:search, params[:search], class: "form-control me-2", type: "search", autocomplete: "off") %>
      <%= submit_tag("Search", :name => nil, class: "btn btn-outline-success") %>
    <% end %>
    
  </div>
</nav>